<nav class="navbar navbar-expand-lg navbar-dark bg-primary p-3">
    <%= link_to "Home" , root_path, class:"navbar-brand" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-between" id="navbarNav">
        <div class="navbar-nav">
            <%= link_to "レストラン", restaurants_path, class:"nav-link nav-item active" %>
        </div>
        <div class="navbar-nav">
            <% if user_signed_in? %>
                <%= link_to current_user.name, edit_user_registration_path, class:"nav-link nav-item active" %>
                <%= link_to "ログアウト", destroy_user_session_path, class:"nav-link nav-item active", method: :delete  %>
             <% else %>
                <%= link_to "サインイン" , new_user_session_path, class:"nav-link nav-item active" %>
                <%= link_to "ユーザ登録" , new_user_registration_path, class:"nav-link nav-item active" %>
             <% end %>
        </div>
    </div>
</nav>